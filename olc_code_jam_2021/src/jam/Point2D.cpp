#include "Point2D.h"

namespace jam
{
	/*template <class T>	
	Point2D<T>::Point2D()
	{
		Point2D(0, 0);
	}

	template <class T>
	Point2D<T>::Point2D(const Point2D<T>& source)
	{
		this->x = source.x;
		this->y = source.y;
	}

	template <class T>
	Point2D<T>::Point2D(T x, T y)
	{
		this->x = x;
		this->y = y;
	}

	template <class T>
	T& Point2D<T>::operator=(const T& other)
	{
		// Guard self assignment
		if (this == &other)
			return *this;

		// assume *this manages a reusable resource, such as a heap-allocated buffer mArray
		this->x = other.x;
		this->y = other.y;
		return *this;
	}
	*/
}